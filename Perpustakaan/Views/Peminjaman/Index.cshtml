@model IEnumerable<Perpustakaan.Models.Peminjaman>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.User.IDUser)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.User.Password)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.IDBuku)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.TanggalMulai)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.TanggalSelesai)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.User.Password)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.IDBuku)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.TanggalMulai)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.TanggalSelesai)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.IDPeminjaman }) |
            @Html.ActionLink("Details", "Details", new { id=item.IDPeminjaman }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.IDPeminjaman })
        </td>
    </tr>
}

</table>
